{
    "name": "inference",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "node -e \"console.log('[inference] Running in Docker container on :8000 - skipping local dev')\"",
        "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000"
    }
}