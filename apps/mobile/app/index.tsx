// Dashboard — lists all matches, links to detail
// TODO: wire up useMatches hook and UploadButton
import { View, Text } from "react-native";

export default function DashboardScreen() {
  return (
    <View>
      <Text>Dashboard — match list goes here</Text>
    </View>
  );
}
