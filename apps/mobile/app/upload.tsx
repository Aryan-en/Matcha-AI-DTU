// Upload screen — picks a video file and sends it to the orchestrator
// TODO: implement with expo-document-picker + upload logic
import { View, Text } from "react-native";

export default function UploadScreen() {
  return (
    <View>
      <Text>Upload — file picker goes here</Text>
    </View>
  );
}
